# Playwright (auth state, test results, reports)
playwright/*

# Ouroboros config (contains credentials — do not commit)
.ouroboros/config.json

# Node
node_modules/
dist/

# Playwright
test-results/
playwright-results/
playwright-report/
blob-report/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

.playwright-mcp
.ouroboros
.vscode

# Framework source — tracked (base, components, fixtures, helpers, utils, cli, index.ts)
# Domain-specific source — NOT tracked (generated by agents)
src/docs/
src/pages/
src/tests/